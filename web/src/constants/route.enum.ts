export enum EROUTE_PUBLIC {
  LOGIN = "login",
}

export enum EROUTE_PROTECTED {
  DASHBOARD = "dashboard",
  OFFICIALS = "officials",
  PROJECTS = "projects",
  REQUEST = "request",
  RESIDENTS = "residents",
  NOTIFICATIONS = "notifications",
  EVENTS = "events",
}

export enum EREPORT {
  REQUEST_REPORTS = "request-reports",
  RESIDENT_REPORTS = "resident-reports",
}
